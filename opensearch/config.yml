_meta:
  type: "config"
  config_version: 2

config:
  dynamic:
    http:
      anonymous_auth_enabled: false

    authc:
      # Kept for the one-shot init container and emergency/admin access.
      basic_internal_auth_domain:
        description: "Internal users for bootstrap/admin access"
        http_enabled: true
        transport_enabled: true
        order: 0
        http_authenticator:
          type: basic
          challenge: false
        authentication_backend:
          type: internal

      # Requests from the Java app arrive with:
      # Authorization: Bearer <Keycloak access token>
      openid_auth_domain:
        description: "Keycloak OIDC access tokens"
        http_enabled: true
        transport_enabled: false
        order: 1
        http_authenticator:
          type: openid
          challenge: false
          config:
            subject_key: preferred_username
            roles_key: roles
            openid_connect_url: >-
              http://keycloak.localhost:8080/realms/demo/.well-known/openid-configuration
            required_issuer: http://keycloak.localhost:8080/realms/demo
            required_audience: opensearch
        authentication_backend:
          type: noop
